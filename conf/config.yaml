# @package _global_

# Root config: declares the experiment config group and its default.
# Override on the CLI with: experiment=<name>

defaults:
  - experiment: classic_triton_state
  - _self_
  - override hydra/launcher: joblib

hydra:
  launcher:
    n_jobs: 4