# @package _global_

# Data variant: classical NSDB data only (no Voyager).
# Bundles the collection, dynamics, and kernels for this data selection.

defaults:
  - /collection: atanas_triton_generated
  - /dynamics: classical
  - /kernels: classical