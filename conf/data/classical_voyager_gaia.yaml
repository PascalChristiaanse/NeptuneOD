# @package _global_

# Data variant: classical NSDB data + Voyager 2 data.
# Bundles the collection, dynamics, and kernels for this data selection.

defaults:
  - /collection: classical_plus_voyager_plus_gaia
  - /dynamics: classical_voyager_gaia
  - /kernels/voyager
  - /kernels/gaia